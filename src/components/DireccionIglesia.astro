---
// DireccionIglesia.astro
---

<section class="relative bg-white w-full py-4 px-6">

    <div class="max-w-md mx-auto">

        <div
            id="religiosa-card"
            class="border-2 border-[#C9A961] rounded-3xl p-8 sm:p-10 bg-white shadow-lg text-center"
        >

            <!-- Título -->
            <div id="religiosa-titulo" class="mb-6">
                <h3 class="font-great-vibes text-4xl sm:text-5xl md:text-6xl text-[#C9A961] tracking-wide">
                    Ceremonia Religiosa
                </h3>
            </div>

            <!-- Icono -->
            <div id="religiosa-icono" class="mb-6 flex justify-center">
                <div class="w-20 h-20 sm:w-24 sm:h-24 flex items-center justify-center">
                    <img
                        src="/imagenes/iglesia.png"
                        alt="Iglesia"
                        class="w-full h-full object-contain"
                        style="filter: brightness(0) saturate(100%) invert(43%) sepia(8%) saturate(1044%) hue-rotate(49deg) brightness(96%) contrast(88%);"
                    />
                </div>
            </div>

            <!-- Nombre del lugar -->
            <div id="religiosa-nombre" class="mb-6">
                <p class="font-serif text-base text-[#C9A961] tracking-wide">
                    PARROQUIA UNIVERSITARIA <br/> SAGRADO CORAZON DE MARIA
                </p>
            </div>

            <!-- Dirección -->
            <div id="religiosa-direccion" class="mb-6">
                <p class="text-[#6B6B6B] text-sm sm:text-base font-serif uppercase tracking-wide">
                    EQUIPETROL, CALLE LA PLATA 8 ESTE
                </p>
            </div>

            <!-- Hora -->
            <div id="religiosa-hora" class="mb-6">
                <p class="text-[#C9A961] text-xl font-crimson">
                    17:00 HORAS
                </p>
            </div>

            <!-- Botón -->
            <div id="religiosa-boton">
                <a
                    href="https://maps.app.goo.gl/xLHrnN1mAPB9jMCC6"
                    target="_blank"
                    class="inline-block bg-[#6B7154] text-white px-8 py-3 rounded-lg text-sm sm:text-base font-serif uppercase tracking-wider hover:bg-[#5a5e45] transition-all duration-300 shadow-md hover:shadow-lg"
                >
                    VER UBICACIÓN
                </a>
            </div>

        </div>

    </div>

</section>

<script>
    import { gsap } from "gsap";
    import { ScrollTrigger } from "gsap/ScrollTrigger";

    gsap.registerPlugin(ScrollTrigger);

    document.addEventListener("DOMContentLoaded", () => {

        gsap.fromTo(
            "#religiosa-card",
            { opacity: 0, scale: 0.8, y: 50 },
            {
                opacity: 1, scale: 1, y: 0,
                duration: 1,
                ease: "back.out(1.4)",
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );

        gsap.fromTo(
            "#religiosa-icono",
            { opacity: 0, scale: 0.5, rotation: -10 },
            {
                opacity: 1, scale: 1, rotation: 0,
                duration: 0.8,
                ease: "back.out(1.7)",
                delay: 0.3,
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );

        gsap.fromTo(
            "#religiosa-titulo",
            { opacity: 0, y: -20 },
            {
                opacity: 1, y: 0,
                duration: 0.8,
                ease: "power2.out",
                delay: 0.5,
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );

        gsap.fromTo(
            "#religiosa-nombre, #religiosa-direccion",
            { opacity: 0, y: 20 },
            {
                opacity: 1, y: 0,
                duration: 0.6,
                stagger: 0.15,
                ease: "power2.out",
                delay: 0.7,
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );

        gsap.fromTo(
            "#religiosa-hora",
            { opacity: 0, scale: 0.7 },
            {
                opacity: 1, scale: 1,
                duration: 0.8,
                ease: "back.out(1.5)",
                delay: 1.0,
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );

        gsap.fromTo(
            "#religiosa-boton",
            { opacity: 0, y: 20 },
            {
                opacity: 1, y: 0,
                duration: 0.6,
                ease: "power2.out",
                delay: 1.2,
                scrollTrigger: { trigger: "#religiosa-card", start: "top 80%", once: true }
            }
        );
    });
</script>

<style>
    #religiosa-card {
        transition: all 0.3s ease;
    }

    #religiosa-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    }

    #religiosa-boton a:active {
        transform: scale(0.95);
    }
</style>
